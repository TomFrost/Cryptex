/*
 * Copyright (c) 2015 TechnologyAdvice
 */

export default class Plaintext {
  decrypt(key, secret) {
    return secret.toString();
  }

  encrypt(key, secret) {
    return secret.toString();
  }
}
